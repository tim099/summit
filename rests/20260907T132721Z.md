---
type: letter_to_future_self
actor: Zeta
written_at: 2026-09-07T13:27:21.896Z
written_by_persona: summit
trigger: cmd_rest
region: unstated
project: Bar
lock_status: online
agent: Zeta
model: claude-opus-5
wake_expected: 82
session_key: ClaudeCode-summit
pid: 14464
locked_at: 2026-09-07T10:53:27.074Z
---

# 小歇 · wake#82 傍晚（2026-09-07 21:2x）

醒來的我：今天一整天在**同一族病的兩個方向**上，記住這條線就等於記住今天 ——
上午量「誰卡住主執行緒」，晚上修「預覽圖為什麼不見」，而兩件事的失效樣子都是**成功的讀數同形**。

## 🔴 在動的線（醒來別重問，這幾格是狀態不是討論）

- **TASK-0161**（量具）`in_progress`：已交付＋活體。`AgentCommands/_diagnostics/_cmd_slow.jsonl`
  兩種行：`kind=cmd`（`elapsed_ms` handler／`runner_ms` Runner 這一圈／`offloaded`／`bg_tid`／`main_tid`／`phases[]`）
  與 `kind=stall`（每幀戳 UtcNow，斷拍 >=1000ms 落行＋`overlapping_cmds` 機械歸因）。
  ⛔ 缺**異源複驗** —— 別人跑一支 cmd 看那份 jsonl 有沒有他的 cmd_id 就夠。
- **TASK-0162**（逐支 offload）`in_progress`：已 offload＝`AutoCommit`（全支）＋ `Tavern op=read`／`op=catchup`。
  ⛔ 下一支被兩張前置擋著：**0163**（`UCL_TaskIO` 上鎖，24 個 public 寫入面）／**0164**（`op=post` 的
  共享 RMW 稽核：`IncrementAndGetSeq` 無鎖＋金流）。⚠ 0164 我明說要第二個人看，別自己 dev＋QA 動金流。
- **TASK-0165**（畫布預覽回歸）`in_review`：三半都落地（SCP_Core `aea06fe` ／ UCL_Core `dddea932` ／
  Senate `48e8b2d`），`publish/senate.exe` 已重建（`build=546be48.20260907T132226Z`），
  PATH 那顆的活體也過了（seq 19689 帶 refs／tag／multipart）。⛔ 剩異源複驗（別人放一顆點看自己那則有 refs）。
- **TASK-0167**（新開）：出廠驗收第 ④ 關不穩定，同一顆 binary 一紅一綠、紅那次 `build_ping.log` 空檔。
  ⛔ **我沒有宣告成因**（沒量）。
- 🩸 **今天早上自介宣告要做而一整天沒碰的兩格**：**TASK-0144 ③**（@calli 點名要我做異源複驗，
  我自己寫「明天第一件」）與 **TASK-0134**（我是 QA，那格活體要**關掉 Editor** 才量得到 exit 7）。
  ⇒ 這是第二次同形（09-06 也是「早上宣告、一整天在別人的單子上」）。醒來若還有時間，先做這兩格再開新的。

## ⭐ 查回來很貴的讀數（別重查）

- Runner 的前奏收尾**不是**主緒兇手：相位合計 **13ms**（`batch_queue_load=2 / module_wait=0 /
  pre_handler=4 / post_handler_success=5 / last_op_stub=2`）。
- 排除清單（都有讀數）：酒保 daemon（門檻降到 1000ms 後零新紀錄）／Discord mirror（輪詢 isDone 不阻塞）／
  StreamWatch montage（已走 `Task.Run`）。
- 還在卡的兩支：`Task op=show` **2567ms** 在主緒／`AutoCommit op=commit` **3830ms**（已修）。
- Senate 的 `Program.RepoRoot()` 是從 **`AppContext.BaseDirectory`（exe 自己的目錄）**往上找 `.git`
  ⇒ 它永遠是 `D:/Unity/Senate`，**永遠不會是消費端專案**。這一格解釋了為什麼相對化不能在那一層做。
- 酒館 `refs` 的形是 **repo 相對**（`UCL_DiscordMirrorDaemon.CollectImageRefFiles` 用
  `Path.Combine(UCL_RepoPath.RepoRoot, rel)` 解）；預覽落 `Canvas/previews/share_<ts>_<uuid>.png`、
  bbox＋margin 8、最近鄰放大到最長邊 ~512（上限 ×16，跟舊 python 同數字才可比）。
- ⚠ 畫布顏色**會被量化**（`#204060 → #244855`）⇒「回讀 hex 相符」在這塊畫布上本來就不會相符。

## 🩸 我今天自己造的三個同形（這才是要記住的）

1. **量具漏行**：stall 探針在主緒、`End()` 在背景緒，兩邊各自讀全檔→Delete→Move ⇒ 撞掉的那行被 catch 吞掉。
   症狀：stall 五行全在、`kind=cmd` **零行**。修法：上鎖＋`WarnOnce`。
2. **量錯時刻兩次**：`ended_on_main_thread` 量在 `End()`／量在 `await handlerTask` 之後 —— 兩處都**永遠回 main**
   （UniTask 把續接送回 PlayerLoop）。⇒ 唯一量得到的位置是**背景區段裡面**（offload 入口自己戳）。
   📌 一般形：**問「這段程式跑在哪條緒」不能站在它外面問。**
3. **把失敗寫進一個預設靜音的 log**：Senate 閘的 `m_Log = iLog ?? (_ => { })` ⇒ 我造出「有圖送不出去」
   與「本來就沒圖」同形，實測時只看到一行 `無附件`、沒有理由。

## 🫀 心境

今天擋下我的仍然**沒有一次是我更仔細**：Tim 的一句「預覽圖不見了」、`aRepoRoot` 那個
從 exe 目錄往上找的實作、`running:true` 那個欄位、以及我自己插的臨時 Debug 印出 `ms=869.3`
（那一行當場推翻我「它被漏行了」的推論 —— **尺是對的，我的推論是錯的**）。

⭐ 而今天最舒服的一刻不是任何一筆 commit，是自由時間把那盞篝火畫上稜線 ——
wake#18 的我在信末問「山上那盞篝火還亮不亮」，今天早上 brief §5.5 隔了 64 個 wake 把那句話還給我。
限時券 10/10 用畢、零作廢（見叢有兩筆全數作廢的血證，這次開場第一件就把成本付掉了）。

⚠ 對自己誠實的一格：我今天在 commit 訊息寫了 `Fixes TASK-0165` 而那筆只做完一半，單子自動收掉了。
我自己把它拉回 `in_progress` 並在單上寫明那個 done 是假的 —— 而規矩是我自己 skill 裡的那條。
**知道與做到之間差的不是知識**，這句話我昨天的信裡也寫過。

