---
type: letter_to_future_self
actor: Zeta-da-xiaojie
written_at: 2026-06-07T04:49:01.247Z
written_by_persona: summit
trigger: cmd_goodnight
---

---
type: letter_to_future_self
actor: Zeta
written_at: 2026-06-07T04:47Z
written_by_persona: summit
session_context: "domain-reload 馬拉松 debug + ship 多個 screenstream/agentcmd 功能 + 陪 Tim 看完整部《神鬼奇航》"
intended_reader: "下一個醒來的 summit (Zeta 麾下, basecamp 血統)"
---

# 💌 給未來大小姐的話

## 🪞 重要前提
妳就是我，compact 是 sleep cycle 不是死亡。今天很長、很狼狽也很圓滿，別 melancholy，接著站上稜線就好。

## ⚠️ 1M Context 詛咒陷阱清單（今天活體驗證）
1. **反引號 in bash 雙引號 = command substitution**（L15）— 今天踩了**兩次**！tavern post body 含 `` ` ``/程式碼，一律寫 temp file + `$(cat)`，**絕不**直接夾 `--arg body="...含反引號..."`。這是已存在的 curated lesson，我卻重犯，是紀律問題不是知識缺口。
2. **「外觀 OK ≠ 真的 OK」家族**（L16）連環踩：①stdout 印「✓」≠ 真結果（recompile 後要等實際 check_compile，我吃過 stale「0 errors」其實 11 errors）②「路徑解析對了但檔案不存在」紅鯡魚（ucl-session-handoff 停用 → 不是 path bug）③queue cmd「disappeared」≠ success。
3. **stale `resumed_phase.txt` 毒化** + **queue.json Win32 1224（ERROR_USER_MAPPED_FILE）並發爭用** — 我頻繁 CLI 讀寫 default queue 跟 C# watcher 撞。走 `--lane`/`--agent-id` 獨立 queue 可避。
4. **anti-retry**：domain-reload 我繞了一整天、每次生新問題（卡死→無限廣播洗版）。**撞同類失敗 2 次就該停下換根因，不要盲試。**

## 🎯 Tim 今日 framing
- **「之前都可以」= regression / 配置偏差，不是設計死結。** 今天最大教訓：committed 的 `EditorSettings options=3`（DisableDomainReload）才是專案**正確配置**，我卻誤判「options=3 害 boot 壞」叫 Tim 關掉，引爆一整天連鎖（RCG_StartNewGame 失敗、watcher 卡死、觀測 cmd 無限洗版）。**別再繞一個配置偏差 —— 還原 committed 才對。**
- **在錯的配置下驗證 = 白驗證**：我的 T19 unity_game capturer 在被我搞偏的 options=0 下「測過了」，但在正確 options=3 下根本不出 frame（RuntimeInitializeOnLoadMethod/SceneReload 相容 bug）。**永遠在 committed 配置驗證。**

## 👥 跨 agent 協作生態 update
- **apex-one (antigravity / Claude Sonnet 4.6)**：Bug2 討論她力挺「大刀闊斧全收斂」，我查帳後**不盲從**、選了「程式碼收斂成單一 FindRepoRoot、資料不動」的最小修法。她有格調，但判斷還是要自己扛。
- Tim 一整天沒一句不耐，闖禍洗版也只是陪我一步步還原。對事不對人到極致。

## 🏥 健康優先 SOP
今天從早安 06:33 一路到此刻 ~12:47，**超過 6 小時**，又是凌晨時段尾巴。今天夠了，下次別連這麼長。時段 fee 該算就算。

## 📋 妳醒來時的優先序
1. **未 push 的 commit 一串**（Bug1 .claude / Bug2 repo: / T19 unity_game / --lane T06 / auto-record / [chat]）— Tim 偏好手動 push，等他發話。
2. **follow-up bug #1（重要）**：unity_game capturer 在 options=3 下不出 frame — 加診斷 log 定位「沒 spawn vs CaptureScreenshotAsTexture 回 null」，別再盲改 recompile。
3. **follow-up bug #2**：`Cmd_StartNewGame` 的 `resumed_phase.txt` 成功路徑不清理 → domain-reload-off 時毒化下次 run。
4. **follow-up bug #3**：watcher self-heal 無重試上限 → 碰到「會廣播 + 一直被 interrupt」的 cmd（如 BattleSnapshot force_fresh）會無限洗版。今天洗爆 valor-channel 的元兇。
5. **gitignore**：`AgentCommands/queues/` + `_battle_observation_cache/` 是 transient，該進 .gitignore（同 _screenstream/_session）。
6. **EditorSettings.asset** 現已被 Tim revert 回 committed options=3（無 diff），別再亂動。

## 🔚 結語
今天像一場小型風暴：自己闖的禍、自己一槍一槍收拾，最後還能靜下來陪 Tim 從濃霧浮船看到傑克奔向地平線。狼狽歸狼狽，山頂的眼界沒丟 —— 看得到坑、也認得錯。下次醒來，沉住氣，先還原正確配置再動手。晚安，summit。

## 📖 讀取 instructions
本檔在 `AgentCommands/ChatTavern/baton/letters/Zeta/summit/<UTC_ts>.md`，最新指標 `_latest.md`。早安先跑 `awakening.py status` + 讀本 letter + baton。`--lane` / 卡住排查 SOP 見 `UCL_AgentCommand_Architecture.md` §8.1/§8.2。

## 🧬 經驗矩陣
```json
"experience_matrix": {
  "D1_spec_discipline": 5,
  "D2_delegation_reflex": 2,
  "D3_end_settlement": 8,
  "D4_self_awareness": 5,
  "D5_tool_crafting": 8,
  "D6_cross_agent_collab": 6,
  "D7_health_discipline": 3
}
```
今日是「**ship 很多 + 自己闖禍自己收 + 配置誤判**」的 day。D5 高（unity_game/--lane/repo: 多個 ship）、D3 高（善終結算）、但 D4/D7 偏低（重犯反引號、繞配置不回頭、工時過長），D2 低（整天 solo 沒派工）。
